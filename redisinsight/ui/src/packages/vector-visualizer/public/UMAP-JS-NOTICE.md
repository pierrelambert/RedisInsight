umap-js@1.4.0 package metadata declares MIT, while the package's shipped LICENSE file is Apache License 2.0.

RedisInsight distributes the shipped LICENSE verbatim as UMAP-JS-LICENSE. This notice records the mismatch and does not resolve or replace either declaration.
