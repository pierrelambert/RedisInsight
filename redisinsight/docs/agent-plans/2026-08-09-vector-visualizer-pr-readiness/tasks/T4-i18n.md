# T4 — Add i18n for VectorFieldPicker and VectorSetKeySubheader

Role/provider: Implementor / Codex. Requested `gpt-5.4` medium; actual dispatch `gpt-5.6-terra` medium, non-inherited. Commit allowed: no.

Owned production files: `VectorFieldPicker.tsx`, `VectorSetKeySubheader.tsx`, and additive entries only in UI locale JSON files. T5 concurrently adds a different locale key; preserve every concurrent addition. Reports may be written only to `T4-report.md`.

Read `.ai/skills/i18n/SKILL.md`, testing, and code-quality rules. In `VectorFieldPicker.tsx`, import `useTranslation` from `react-i18next`, initialize `t`, and replace the select, loading, error, empty, and cancel strings with keys under `vectorVisualizer.fieldPicker.*`. In `VectorSetKeySubheader.tsx`, replace the Visualize text with `t('vectorVisualizer.actions.visualize')`. Add all six English source keys and matching Bulgarian keys according to the repository parity convention; empty Bulgarian values are permitted. Do not edit package/native visualizer code or `defaultRoutes.ts`.

Verify `npm run lint`, `npm run type-check`, i18n parity/check if available, and classify only new diagnostics from owned files. Do not repair the aggregate baseline. Report exact commands/exits, files, key names, blockers, and no-stage/no-commit statement.
